# blinktree (retired)

This personal link page is **retired**. Projects and social profiles now live on **[barrett.software](https://barrett.software/)**.

GitHub Pages cannot emit an HTTP 301 from this repo (no custom domain). The published page shows a short retired notice, then sends visitors to Barrett Software via meta refresh and `location.replace`.

The original open-source link-in-bio template remains in git history if you want to fork it.
